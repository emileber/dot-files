alias gs='git status $*'
alias gl='git l $*'
alias fhere="find . -name "

alias ps="ps aux"

alias mkdir="mkdir -pv"    # Create any parent directory

alias histg="history | grep"

# Pretty print the PATH env var
alias path='echo $PATH | tr -s ":" "\n"'
